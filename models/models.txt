To download models trained on the operational TEL dataset navigate to the verenie/telmodels repo.  To test these models on your own datasets follow the format of the data/ood_tel folder and specify it in the
testnetwork.py module.
